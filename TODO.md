# TODO

- [ ] Update `src/components/AboutSection.jsx`:
  - [ ] Convert layout to 50/50 split (image left + text right) using `public/images/about.webp`
  - [ ] Keep “Kerala's Trusted Partner in Vertical Mobility” block on the right
  - [ ] Replace the dense “Guaranteed Safety and Licensing” paragraphs with a 2x2 icon grid using the 4 provided items/titles
- [ ] Update `src/components/StatsBar.jsx`:
  - [ ] Restyle into a slim, full-width, dark horizontal banner
  - [ ] Use white text for stats
- [ ] Run `npm run dev` (or `npm run build`) to verify the UI renders correctly
